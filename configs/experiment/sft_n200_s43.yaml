# @package _global_
# The SFT comparator (see sft_n200.yaml) at seed 43.
defaults:
  - sft_n200
  - _self_

task_name: "sft_n200_s43"
tags: ["${task_name}", "sft", "comparator", "v2", "11ch", "seed${seed}"]

seed: 43
