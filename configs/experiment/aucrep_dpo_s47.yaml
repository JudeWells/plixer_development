# @package _global_
# Independent-seed rebuild of a DPO ensemble member, seed 47. Pairs with aucrep_frozen_s47
# so a rebuilt ensemble spans the same regimes as A (frozen + DPO) at disjoint seeds.
defaults:
  - rl_dpo_auc_1e5_s42
  - _self_

task_name: "aucrep_dpo_s47"
tags: ["${task_name}", "ensemble_replicate", "v2", "11ch", "seed${seed}"]

seed: 47
